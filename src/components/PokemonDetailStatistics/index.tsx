import { IPokemon } from "@features/types";

const PokemonDetailStatistics: React.FC<IPokemon> = ({
  ...props
}: IPokemon) => {
  return <div>Pokemon Detail Statstics</div>;
};

export default PokemonDetailStatistics;
