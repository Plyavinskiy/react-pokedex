import { IPokemon } from "@features/types";

const PokemonDetailProfile: React.FC<IPokemon> = ({ ...props }: IPokemon) => {
  return <div>Pokemon Detail Profile</div>;
};

export default PokemonDetailProfile;
